{
  "name": "metadata",
  "private": true,
  "type": "module",
  "scripts": { 
    "build": "bun build --minify --outdir=./js ./src/tabview.js"
  },
  "dependencies": {
    "@nextcloud/files": "^4.0.0",
    "@nextcloud/router": "^3.1.0"
  }
}
